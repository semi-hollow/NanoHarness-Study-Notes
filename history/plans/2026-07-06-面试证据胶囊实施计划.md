# Interview Evidence Capsule Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a compact interview evidence capsule and learning path so Agent Forge can be studied and presented quickly as a non-toy AI Agent engineering project.

**Architecture:** Keep existing UI structure intact. Add a concise Interview Mode section to the existing evidence view, create one central 30-minute learning document, and route existing technical-defense docs through that document instead of adding many more scattered files.

**Tech Stack:** Python stdlib UI renderer, markdown docs, unittest.

---

### Task 1: Protect The UI Product Effect

**Files:**
- Modify: `tests/test_public_cli.py`
- Modify: `agent_forge/ui.py`

- [ ] **Step 1: Write the failing test**

Add assertions to `test_interview_evidence_view_renders_without_artifacts`:

```python
self.assertIn("Golden Demo Capsule", html)
self.assertIn("30 分钟学习路径", html)
self.assertIn("docs/technical-defense/三十分钟面试准备包.md", html)
```

- [ ] **Step 2: Run the targeted test to verify it fails**

Run:

```bash
python3.11 -m unittest tests.test_public_cli.PublicCliSmokeTest.test_interview_evidence_view_renders_without_artifacts
```

Expected: FAIL because the current HTML has no capsule or 30-minute path.

- [ ] **Step 3: Add minimal UI content**

In `_render_interview_evidence`, add a short `Golden Demo Capsule` section and a `30 分钟学习路径` table before the existing 5-minute demo.

- [ ] **Step 4: Run the targeted test to verify it passes**

Run:

```bash
python3.11 -m unittest tests.test_public_cli.PublicCliSmokeTest.test_interview_evidence_view_renders_without_artifacts
```

Expected: PASS.

### Task 2: Create The Central Learning Route

**Files:**
- Create: `docs/technical-defense/三十分钟面试准备包.md`
- Modify: `docs/technical-defense/评测目录说明与SWE-bench使用入口.md`
- Modify: `docs/technical-defense/demo-evidence/评测目录说明与SWE-bench使用入口.md`
- Modify: `评测目录说明与SWE-bench使用入口.md`

- [ ] **Step 1: Write the central doc**

Create one compact page that gives:

- 30-minute study order.
- Dependency graph between docs and core files.
- What to show in UI.
- What claims are safe or unsafe in interviews.

- [ ] **Step 2: Route existing docs through the central page**

Update `docs/technical-defense/评测目录说明与SWE-bench使用入口.md` so the first item is the 30-minute pack, and make other docs supporting references.

- [ ] **Step 3: Keep demo evidence honest**

Update `demo-evidence/评测目录说明与SWE-bench使用入口.md` to frame the evidence as a capsule: live run when available, offline route when dependencies are missing, and no official resolved-rate claim without official harness.

- [ ] **Step 4: Surface the new route from the top-level README**

Add the 30-minute pack to the docs list near the technical-defense entries.

### Task 3: Verify

**Files:**
- Test: `tests/test_public_cli.py`
- Test: `tests/test_evaluation_comparison.py`

- [ ] **Step 1: Run targeted UI test**

```bash
python3.11 -m unittest tests.test_public_cli.PublicCliSmokeTest.test_interview_evidence_view_renders_without_artifacts
```

- [ ] **Step 2: Run full local tests**

```bash
python3.11 -m unittest discover tests
```

- [ ] **Step 3: Compile package**

```bash
python3.11 -m compileall -q agent_forge
```
